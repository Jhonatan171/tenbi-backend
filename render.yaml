# Archivo: render.yaml (Va junto a tu pom.xml)
services:
  - type: web
    name: tenbi-backend
    env: docker  # Mantiene la selección de "Docker" en la UI de Render
    # Comandos de compilación y ejecución de Maven/Java
    buildCommand: "mvn clean install -DskipTests"
    startCommand: "java -jar target/*.jar"
    
    # Ruta para que Render verifique que la app está viva
    healthCheckPath: "/actuator/health"
